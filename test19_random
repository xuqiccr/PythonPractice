import random
aList = []
i = 0
N = random.randint(2, 100)
while i< N:
    n = random.randint(0, 2**31)
    aList.append(n)
    i = i + 1
    if len(aList) == N:
        print aList
        print '\n'
        break
a = random.randint(1, 100)
bList = random.sample(aList, a)
print sorted(bList)
